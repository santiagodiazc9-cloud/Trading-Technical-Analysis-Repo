# Security Scan — 2026-05-16 (Saturday weekly)

Routine: `routines/7_security_scan.md`
Trigger: scheduled 11:00 AM ET / 15:00 UTC
Mode: read-only. **No code modified.**

## Scope
- 9 Python scripts (`scripts/alpaca_client.py`, `discord_bot.py`, `discord_bot_cloud.py`, `notify.py`, `post_channel_explainers.py`, `git_sync.py`, `research.py`, `dashboard.py`, `vault_index.py`).
- 7 declared dependencies (`alpaca-py`, `pandas`, `numpy`, `ta`, `python-dotenv`, `discord.py`, `httpx`).
- Git history for `.env` and `memory/discord_config.json`.
- File permissions on `memory/` (no secret patterns matched).

## Findings by severity

| Severity | Count | Notes |
|----------|-------|-------|
| CRITICAL | 0     | — |
| HIGH     | 2     | Both in `scripts/discord_bot_cloud.py` — cloud-deploy secret hygiene |
| MEDIUM   | 3     | Input validation gaps (`discord_bot.py` ×2, `alpaca_client.py` ×1) |
| LOW      | 1     | Operational: `ACTION_LOG` has no rotation |
| Positive | 3     | `paper=True` hardcoded, no live-API codepath, no eval/exec/pickle/shell=True/verify=False, `.env` clean in git history, all `httpx` calls have timeouts |

### HIGH
1. `scripts/discord_bot_cloud.py:70` — GH_TOKEN embedded in git remote URL, persisted to `.git/config`.
2. `scripts/discord_bot_cloud.py:85` — Discord bot token written to a `.env` file inside the repo tree.

### MEDIUM
1. `scripts/discord_bot.py:329-339` — `/watchlist add notes:"..."` and `/ask question:"..."` pass raw free-text into queue files later consumed by Claude routines (prompt-injection vector, not shell-injection).
2. `scripts/discord_bot.py:131,158` — `/deny <id> <reason>` interpolates `reason` into `memory/open_positions.md` without stripping `\n`/`\r`/`-->`. Could corrupt the markdown the executor parses.
3. `scripts/alpaca_client.py:307-320` — `buy`/`sell`/`trailing_stop`/`stop` accept `symbol`/`qty` from `argv` with no upper bound, no symbol regex, no notional ceiling. 20%/$20k cap is enforced only by Claude in routines, not at the script boundary.

### LOW
1. `scripts/discord_bot.py:108` — `ACTION_LOG` opened in append mode without rotation.

### Dependency CVE flags (live-feed re-verification needed — knowledge cutoff Jan 2026)
- `httpx>=0.25.0` — CVE-2024-25199 (URL parsing edge case). Recommend bumping floor to `>=0.27.0`.
- `pandas>=2.0.0` — CVE-2024-9880 (ReDoS in `read_csv` engine='python') affects <2.2.2. Recommend bumping floor.
- `alpaca-py`, `numpy`, `ta`, `python-dotenv`, `discord.py` — no known criticals through Jan 2026, but **re-verify against live PyPA + GitHub Advisory feeds**.
- Recommend pinning upper bounds (e.g. `httpx>=0.27,<0.29`) and adding `pip-audit` to the weekly routine.

## Actions taken
**None.** This routine is read-only by design. Findings reported to ClickUp `risk_and_errors`:
- ClickUp `869daw4dd` — HIGH (GH_TOKEN in remote URL)
- ClickUp `869daw4e8` — HIGH (bot token written to `.env`)
- ClickUp `869daw4ff` — MEDIUM/LOW combined

Discord posts (CRITICAL/HIGH alerts + brief summary) could not be delivered: the cloud routine host is missing `memory/discord_config.json` AND `httpx` is not installed (`notify.py` import-errors). Pending Discord messages logged to `memory/pending_discord_updates.md` under the `2026-05-16 15:01 UTC` entry. Once Discord credentials + `httpx` are provisioned, flush from there.

RuFlo MCP unavailable → findings NOT indexed into the `trading-security` namespace this run, so next week's scan cannot detect regressions against this baseline programmatically. Re-run after RuFlo is restored if regression tracking is wanted.

## Confidence
Medium-high. Codebase is small (~2,765 LOC) and clean; the two HIGH findings are localized to one file (`discord_bot_cloud.py`) and are deployment-hygiene issues rather than vulnerabilities in tradeable code paths.

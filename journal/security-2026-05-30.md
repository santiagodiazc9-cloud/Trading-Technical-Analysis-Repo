# Security Scan — 2026-05-30 (Saturday weekly)

Routine: `routines/7_security_scan.md`
Trigger: scheduled 11:00 AM ET / 15:00 UTC
Mode: read-only. **No code modified.**

## Scope
- 17 Python files across `scripts/`, `scripts/daytrader/`, and `api/` (~4,610 LOC). Added since 2026-05-16: `api/main.py`, `scripts/setup_validator.py`, 6 files under `scripts/daytrader/`.
- 14 declared dependencies across `requirements.txt` (8), `requirements-api.txt` (4), `requirements-bootstrap.txt` (3, with `yfinance` overlap). +7 new deps vs prior scan.
- Git history for `.env` (clean — no commits).
- Tracked-file secret scan via `git grep` for `sk-`, `pk_live_`, AKIA, `xoxb-` patterns (no matches).
- `grep` of `memory/` for secret patterns (clean).
- `.gitignore` review (`.env` line 1, `memory/discord_config.json` line 8 — both correctly excluded).

## Findings by severity

| Severity | Count | Notes |
|----------|-------|-------|
| CRITICAL | 0     | — |
| HIGH     | 3     | 2 regressions in `scripts/discord_bot_cloud.py`; **1 NEW** in `api/main.py` (zero-auth + CORS `*` on mutating endpoints) |
| MEDIUM   | 4     | 2 regressions (`discord_bot.py`, `alpaca_client.py`); 2 NEW (`api/main.py` injection, `daytrader/ml_model.py` pickle.load) |
| LOW      | 3     | 1 regression (`ACTION_LOG` rotation); 2 NEW (`load_dotenv` coupling, missing dep upper bounds) |
| Dep CVE  | 3     | httpx 0.25 / pandas 2.0 floors still expose CVE-2024-25199 / CVE-2024-9880 (regression); yfinance 0.2 floor exposes 0.2.40 cache-path-traversal (new) |
| Positive | 7     | `paper=True` hardcoded, no eval/exec/shell=True/verify=False/os.system, all 14 httpx+urllib calls timeout-bounded, `.env` clean in git history, `git_sync.py` SECRET_PATTERNS scanner covers Anthropic/AWS/Discord webhook/Slack/Discord bot token/private key/Alpaca PK·AK |

### HIGH
1. `scripts/discord_bot_cloud.py:70` — `GH_TOKEN` interpolated into git remote URL, persisted to `.git/config`. **REGRESSION from 2026-05-16.**
2. `scripts/discord_bot_cloud.py:85` — `DISCORD_BOT_TOKEN` written to `.env` inside the repo tree. **REGRESSION.**
3. `api/main.py:45-50` + `:203-257` — FastAPI deployed with CORS `*` and zero auth on `POST /session`, `/approve/{id}`, `/deny/{id}`, `/pause`, `/resume`. Any cross-origin webpage Santiago visits can force a trade or pause the agent. Bypasses CLAUDE.md rule 15 (APPROVAL REQUIRED) at the network layer. **NEW.**

### MEDIUM
1. `api/main.py:218-242` — `/approve/{setup_id}` and `/deny/{setup_id}` interpolate `setup_id` + `reason` into `memory/open_positions.md` with no normalization. `\n`/`\r`/`## ` injection corrupts the markdown the executor parses. **NEW.**
2. `scripts/discord_bot.py:131,158` — same control-char injection class via `/deny` reason. **REGRESSION.**
3. `scripts/alpaca_client.py:307-320` — buy/sell/trailing_stop/stop accept symbol/qty from argv with no symbol regex, no qty upper bound, no notional ceiling. **REGRESSION.**
4. `scripts/daytrader/ml_model.py:127` — `pickle.load()` on `models/<symbol>.pkl`. RCE sink if anything can drop a file into that directory. **NEW** (daytrader was not in scope on 2026-05-16).

### LOW
1. `scripts/discord_bot.py:108` — `ACTION_LOG` append-only, no rotation. **REGRESSION.**
2. `api/main.py:39` — `load_dotenv()` silently consumes any `.env` in repo root, including the one HIGH #2 writes. Couples deploy-time hygiene to API runtime. **NEW.**
3. All three `requirements*.txt` files: 14 deps, zero `<` upper-bound constraints. **REGRESSION.**

### Dependency CVE flags (live-feed re-verification needed — knowledge cutoff Jan 2026)
- `httpx>=0.25.0` — CVE-2024-25199 — bump floor to `>=0.27.2,<0.29`. REGRESSION.
- `pandas>=2.0.0` — CVE-2024-9880 (ReDoS in `read_csv` engine='python') — bump floor to `>=2.2.3,<3.0`. REGRESSION.
- `yfinance>=0.2.0` — 0.2.40 fixed a cache-path traversal — bump floor to `>=0.2.40,<0.3`. NEW.
- Others clean through Jan 2026; recommend adding `pip-audit` to the weekly routine.

## Diff vs 2026-05-16
- **Still present (regressions):** both HIGH in `discord_bot_cloud.py`; all 3 MEDIUM (`discord_bot.py`×2, `alpaca_client.py`); 1 LOW (`ACTION_LOG`); httpx + pandas CVE flags; missing dep upper bounds. **0 of 6 prior findings have been remediated in the two-week gap.**
- **Fixed:** none.
- **New:** HIGH #3 (`api/main.py` CORS + unauth POSTs); MEDIUM #1 (api setup_id/reason injection); MEDIUM #4 (pickle.load in daytrader); LOW #2 (`load_dotenv` coupling); yfinance CVE; +7 new deps to track.

## Actions taken
**None.** This routine is read-only by design. Findings reported to ClickUp `risk_and_errors` list (`901217854046`):
- ClickUp `869dgf89z` — HIGH (GH_TOKEN in `.git/config`) — https://app.clickup.com/t/869dgf89z
- ClickUp `869dgf8a5` — HIGH (bot token in `.env`) — https://app.clickup.com/t/869dgf8a5
- ClickUp `869dgf8au` — HIGH (FastAPI unauth + CORS `*`) — https://app.clickup.com/t/869dgf8au
- ClickUp `869dgf8b7` — MEDIUM×4 + LOW×3 combined — https://app.clickup.com/t/869dgf8b7
- ClickUp `869dgf8ba` — Dependency CVE re-verification — https://app.clickup.com/t/869dgf8ba

All 5 tasks created on first attempt (no 502 retries needed this run, in contrast to recurring failures seen on 2026-05-28). ClickUp MCP was queried once for workspace hierarchy to resolve `Risk and Errors` list ID `901217854046` after a transient 502 on two earlier search calls.

Discord posts (CRITICAL/HIGH alerts + brief summary) **could not be delivered**: `memory/discord_config.json` is missing from this cloud routine host (recurring known gap, same as 2026-05-16). Pending Discord messages logged to `memory/pending_discord_updates.md` under the `2026-05-30 15:08 UTC` entry — flush from there once Discord credentials are provisioned in the cloud env.

RuFlo MCP unavailable (per parent task instruction) → findings NOT indexed into the `trading-security` namespace this run. The 5 ClickUp tasks above form the persistent regression baseline for next week's scan instead.

## Repeat-finding posture
**0 of 6 findings remediated since 2026-05-16** (14 days, two weekly cycles). Recommend the next manual session take a fix-pass instead of a research pass:
- Quick wins (1-line fix each): bump httpx/pandas/yfinance floors, drop the `load_dotenv()` line in api/main.py, add log rotation to ACTION_LOG.
- Medium (1-2 hour each): symbol/qty/notional guards in alpaca_client.py CLI; control-char strip in discord_bot.py and api/main.py.
- Largest: refactor discord_bot.py to consume `DISCORD_BOT_TOKEN` from env directly (eliminates HIGH #2 + ties LOW #2 closed).
- Largest, deploy-blocking: api/main.py auth layer + CORS allowlist before any production deploy of the dashboard.

## Confidence
Medium-high. Codebase is small (~4,610 LOC) and clean of the "obvious" sinks (no `eval`, no `shell=True`, no `verify=False`, no SQL). The three HIGH findings are localized to two files (`discord_bot_cloud.py`, `api/main.py`) and are deployment-hygiene + missing-auth issues rather than vulnerabilities inside tradeable code paths. Sub-agent verdict + manual cross-check agreed on every finding.
